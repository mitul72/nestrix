# nestrix

An NES emulator, under development.

## Status

Early work in progress.

## Workspace

- `nestrix-core` — emulator core (CPU, PPU, APU, cartridge, etc.)
- `nestrix-sdl` — native frontend using SDL

A WebAssembly frontend is also planned.

## Build

```sh
cargo build
```
